name: Deploy app

on:
  push:
    branches: [main]
  schedule:
    - cron: '*/30 * * * *'   # refresh community posts and tools every 30 minutes
  workflow_dispatch:          # adds a "Run workflow" button on the Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      # Latest field tools, straight from the nuflow-tools repo, so they can be saved for offline use
      - uses: actions/checkout@v4
        with:
          repository: nuflow-australasia/nuflow-tools
          path: tools-src

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build site folder
        run: |
          mkdir _site
          rsync -a --exclude '.git' --exclude '.github' --exclude 'scripts' --exclude '_site' --exclude 'tools-src' ./ _site/
          mkdir -p _site/tools
          cp tools-src/*.html _site/tools/
          ls _site/tools

      - name: Fetch latest community posts
        env:
          CIRCLE_API_TOKEN: ${{ secrets.CIRCLE_API_TOKEN }}
        run: node scripts/fetch-posts.mjs _site/posts.json

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
